<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>New Title</title>
</head>


<body>

    <div class="container">
        <div class="row">
            <div class="col-6">

                <table>
                    <tr>
                        <th>Teacher Name and Last Name</th>
                    </tr>
                    {{#each all_teachers}}
                    <tr>
                        <td> <a href="/teacher_taught/{{id}}"> {{first_name}} {{last_name}}</a> </td>
                    </tr>
                    {{/each}}
                </table>

            </div>


            <div class="col-6">

                <table>
                    <tr>
                        <th>Available Subjects</th>

                    </tr>
                    {{#each the_sub}}
                    <tr>

                        <td><a href="/subject_taught/{{name}}/">{{name}}</a></td>
                    </tr>
                    {{/each}}

                </table>
            </div>

        </div>
    </div>


</body>

</html>